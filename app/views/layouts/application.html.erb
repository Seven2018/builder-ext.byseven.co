<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Builder by SEVEN</title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>
    <% unless params[:controller] == "pages" && params[:action] == "home" || !user_signed_in? %>
    <div id="left-side">
      <%= render 'shared/left-navbar' %>
    </div>
      <%#= render 'shared/navbar' %>
      <%= render 'shared/flashes' %>
    <% end %>
    <div id="right-side" style="<%= 'padding: 0;' if params[:controller] == 'pages' && params[:action] == 'home' %>">
      <div class='<%= "wrapper" unless params[:controller] == "pages" && params[:action] == "home" %>'>
        <%= yield %>
        <%= javascript_include_tag 'application' %>
        <%= javascript_pack_tag 'application' %>
        <%#= render 'shared/footer' %>
      </div>
    </div>
</html>
