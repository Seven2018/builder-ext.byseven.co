function addMod(modHTML) {
  const emptyPlaceHolder = document.querySelector('.empty-mod');
  if (emptyPlaceHolder) {
    emptyPlaceHolder.remove();
  }
  const mods = document.getElementById('items');
  items.insertAdjacentHTML('beforeend', modHTML);
  document.querySelector('.log-box').innerHTML = '<%= j render "sessions/comments", session: @session %>';
};

<% @session.workshops.each do |mod| %>
  <% if mod.position == 1 %>
    <% @start = @session.start_time %>
  <% else %>
    <% @duration = @session.workshops.find_by(position: (mod.position - 1 )).duration.minutes %>
    <% @start += @duration %>
  <% end %>
<% end %>

// addMod('<%= j render "workshops/mod_card", mod: @workshop, session: @session, start: @start %>');
document.getElementById('mods').innerHTML = '<%= j (render("workshops/index", session: @session)) %>';
