<div class="container-seven">
  <h2>Ajouter une théorie</h2>

  <%= simple_form_for @theory do |f| %>
    <%= f.input :name, label: 'Nom' %>
    <%= f.input :description, input_html: { class: 'editor' } %>
    <%= f.input :links, input_html: { style: 'width: 100%;' }, label: 'External links' %>
    <%= f.input :references, input_html: { class: 'editor2' } %>
    <script>
      ClassicEditor
              .create( document.querySelector( '.editor' ), {
              } )
              .then( editor => {
                                        console.log( editor );
                                } )
              .catch( error => {
                      console.error( error );
              } );
    </script>
    <script>
      ClassicEditor
              .create( document.querySelector( '.editor2' ), {
              } )
              .then( editor => {
                                        console.log( editor );
                                } )
              .catch( error => {
                      console.error( error );
              } );
    </script>
    <p class="center-button"> <%= f.button :submit, 'Save', class: 'btn btn-edit' %> </p>
  <% end %>
</div>
